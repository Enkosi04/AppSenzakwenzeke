package com.example.senzakwenzekeapplication

import androidx.appcompat.app.AppCompatActivity

class HomePage : AppCompatActivity() {
    // Not used - we use XML layout activity_home_page.xml
    // Keep this empty so no error
}